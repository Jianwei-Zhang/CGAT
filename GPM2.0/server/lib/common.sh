#!/usr/bin/env bash

# Portable error and command guards shared by Server shell libraries.

die() {
  echo "ERROR: $*" >&2
  exit 1
}

require_cmd() {
  command -v "$1" >/dev/null 2>&1 || die "Required command not found: $1"
}
